# Frontend
VITE_GAS_BASE_URL=https://script.google.com/macros/s/<YOUR_EXEC_ID>/exec
# VITE_API_TOKEN is legacy; avoid populating real tokens in the repository.


# Backend (set in GAS Script Properties, not here)
# SHEET_ID=
# API_TOKEN=
